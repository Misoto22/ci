{
  "Misoto22/touchstone-hosted-probe": {
    "checks": [
      "test",
      "pr-title / pr-title"
    ]
  },
  "Misoto22/kioku-ios": {
    "checks": [
      "Build",
      "pr-title / pr-title"
    ],
    "note": "Build comes from ci.yml (pull request #1): a bare pull_request trigger with no paths filter and no job-level if:, green on every pull request since, Dependabot #2 included. It runs on the self-hosted macOS runner (kioku-ios-mac), so a pull request cannot merge while that machine is offline. pr-title / pr-title has reported green on #2 through #10 since its caller landed in #1. testflight.yml is still push-to-main and workflow_dispatch only."
  },
  "Misoto22/kioku": {
    "checks": [],
    "note": "Classic branch protection owns main here and already requires check, Console, docs and pr-title / pr-title. Reconcile the two before moving the contexts into this ruleset."
  },
  "Misoto22/harness": {
    "checks": [],
    "note": "Classic branch protection owns main here and already requires secrets, verify (ubuntu-latest) and pr-title / pr-title. Reconcile the two before moving the contexts into this ruleset."
  },
  "Misoto22/touchstone": {
    "checks": [],
    "note": "Classic branch protection owns main here and already requires Python 3.12, Python 3.13, Distribution and pr-title / pr-title. Reconcile the two before moving the contexts into this ruleset."
  },
  "Misoto22/misoto22-site": {
    "checks": [
      "pr-title / pr-title"
    ],
    "note": "No CI context: .github/workflows/ci.yml still carries paths-ignore (content/, docs/, public/, **.md, .gitignore, LICENSE) on both push and pull_request, so a docs-only pull request produces no CI run at all and a required ci, docs, just-check or e2e context would sit pending forever and block the merge. mutation.yml and the harness-rendered misoto-harness.yml (rules) are paths-filtered too. pr-title / pr-title is unconditional and has reported green on #400, #401 and the updated Dependabot #393 since its caller landed in #399."
  },
  "Misoto22/folio": {
    "skip": true,
    "note": "Left alone: folio already runs a ruleset named main with three required contexts and an admin bypass (RepositoryRole 5). Replacing it from the fleet default would be a net loss, so its branch protection moves with its release-please migration instead."
  },
  "Misoto22/career-ops": {
    "checks": [
      "pr-title / pr-title"
    ],
    "note": "pr-title / pr-title only. It reported green on Dependabot #17 ('build(deps): bump actions/stale from 10 to 11') after an `@dependabot rebase` on 2026-09-18, the first report since its caller landed in #19. test is the only unconditional pull_request CI job and it is red for a pre-existing reason (node test-all.mjs --quick fails on main, not on any one pull request), so it stays out. label and welcome are pull_request_target-only and welcome fires on `opened` alone, so neither reports on every pull request."
  },
  "Misoto22/skills": {
    "skip": true,
    "note": "Left alone: skills protects its default branch with its own ruleset, 'Protect default branch'. A second ruleset named main would be a duplicate gate on the same branch with nothing to keep the two in step."
  },
  "Misoto22/llm-gateway": {
    "checks": [
      "check",
      "pr-title / pr-title"
    ]
  },
  "Misoto22/kioku-ui": {
    "checks": [],
    "note": "Classic branch protection owns main here and requires check, build, accessibility and consumers with strict: false, deliberately. This ruleset stays []: any context listed here would bring this script's strict_required_status_checks_policy: true, and the most restrictive rule wins, so every kioku-ui pull request would have to be up to date with main. pr-title / pr-title goes in through the classic protection instead, with POST repos/Misoto22/kioku-ui/branches/main/protection/required_status_checks/contexts, which adds one context and leaves strict: false alone. Do that only once the context has reported on a kioku-ui pull request. On 2026-09-18 it had not: no pull request had been opened since the caller landed in #28, and there was no Dependabot pull request to rebase."
  },
  "Misoto22/Shiplog": {
    "checks": [],
    "note": "No required context yet. Verify cannot be required: pull request #4 (merged) put paths-ignore (**.md, docs/**, LICENSE, .gitignore) on both triggers of ci.yml to cap hosted macOS minutes, so a docs-only pull request produces no run and a required Verify would sit pending forever. pr-title / pr-title has a caller since #4, but the only pull request since, Dependabot #5, fails it: 'Bump actions/checkout from 5.1.0 to 7.0.1 in the workflow-actions group' carries no Conventional Commit type, because .github/dependabot.yml sets no commit-message prefix. Shiplog pull request #6 adds `prefix: chore`; once it merges and Dependabot recreates #5, require pr-title / pr-title if that run is green."
  },
  "Misoto22/trading-research": {
    "checks": [
      "verify",
      "pr-title / pr-title"
    ]
  },
  "Misoto22/ai-investment-research-workflow": {
    "checks": [
      "test"
    ],
    "note": "pr-title not yet reported: .github/workflows/pr-title.yml is on main but no pull request has been opened since it landed, and on 2026-09-18 there was no open pull request to rebase either, so the context does not exist yet. Add it once a pull request has reported it."
  },
  "Misoto22/polymarket-edge-lab": {
    "checks": [],
    "note": "Classic branch protection owns main here and already requires 'format, lint, types, tests (Python 3.14)' and pr-title / pr-title. Reconcile the two before moving the contexts into this ruleset."
  },
  "Misoto22/misoto22-admin-ios": {
    "checks": [],
    "note": "No required context: the only workflow is the harness-rendered misoto-harness.yml, and since harness 0.4.0 (adopted in #7) its pull_request trigger is restricted to the paths the drift check reads (.rulesync/**, rulesync.jsonc, rulesync.lock.json, the root and nested AGENTS.md / CLAUDE.md / GEMINI.md, .cursor/rules/**, and the workflow file itself), so `rules` does not report on every pull request and can never be required. There is no pr-title caller either — .github/workflows/pr-title.yml does not exist on main. Add one; require pr-title / pr-title once it has reported green on a later pull request."
  },
  "Misoto22/misoto22-admin": {
    "checks": [
      "pr-title / pr-title"
    ],
    "note": "`rules` cannot be required: the harness-rendered misoto-harness.yml has been paths-filtered since harness 0.4.0 (adopted in #11). There is still no pull-request build job — deploy.yml's validate job runs on push to main only — so pr-title / pr-title, green on #10, #11 and #12 since its caller landed in #9, is the only context that reports on every pull request. Re-survey once a pull-request CI job lands."
  },
  "Misoto22/portrait-lora-pipeline": {
    "checks": [
      "Core (ubuntu-latest)",
      "Core (windows-latest)",
      "body-and-verification",
      "review-resolution",
      "pr-title / pr-title"
    ]
  },
  "Misoto22/zhaojian": {
    "checks": [
      "pr-title / pr-title"
    ],
    "note": "No CI context: .github/workflows/ci.yml still carries paths-ignore (**.md, docs/, LICENSE, .gitignore) on both push and pull_request, so a docs-only pull request produces no CI run at all and a required check would sit pending forever and block the merge. pr-title / pr-title is unconditional and reported green on Dependabot #114, the first pull request after its caller landed in #113."
  },
  "Misoto22/eoi-points-calculator": {
    "checks": [
      "verify",
      "deploy",
      "pr-title / pr-title"
    ],
    "note": "deploy carries no job-level if: — it runs on every pull request and its first step no-ops with a step summary when the three Vercel secrets are absent, which is why it is green on Dependabot pull requests too."
  },
  "Misoto22/cvtailors": {
    "checks": [
      "rust",
      "frontend",
      "pr-title / pr-title"
    ]
  },
  "Misoto22/slatecourt": {
    "checks": [
      "changes",
      "pr-title / pr-title"
    ],
    "note": "changes is the only unconditional CI job: api, web and contract are gated on its dorny/paths-filter outputs, e2e needs web, deploy is push-only, and ios.yml is paths-filtered to apps/ios. pr-title / pr-title reported green on Dependabot #90 ('ci: bump dorny/paths-filter from 4.0.1 to 4.0.3') after an `@dependabot rebase` on 2026-09-18, the first report since its caller landed in #91."
  },
  "Misoto22/servo-map": {
    "checks": [
      "Typecheck",
      "Lint",
      "Test",
      "Build Web"
    ],
    "note": "pr-title / pr-title not required: its caller landed in #19, and Dependabot #20 fails it even after `@dependabot recreate` on 2026-09-18 — 'chore: Bump the actions group across 1 directory with 3 updates' breaks the lowercase-subject rule although .github/dependabot.yml sets the chore prefix. The prefix does not decide the case: dependabot-core's PrNamePrefixer capitalises when no Dependabot commit is on main and every recent Conventional Commit message has ': ' followed by a capital somewhere, and every commit here carries a Co-Authored-By trailer. Retitle #20 to 'chore: bump …' before squash-merging it; later titles then follow that last Dependabot commit. Require the context once it has reported green."
  },
  "Misoto22/kairos": {
    "checks": [
      "Backend (ruff + pytest)",
      "Frontend (lint + vitest)",
      "pr-title / pr-title"
    ],
    "note": "docs.yml is paths-filtered (docs/, mkdocs.yml, docs.sh, start.sh, Makefile, its own workflow file), so Build MkDocs and Lint docs.sh do not report on every pull request and Deploy to GitHub Pages is push-to-main only."
  },
  "Misoto22/kaisetsu-pipeline": {
    "checks": [
      "ci / python-ci"
    ],
    "note": "pr-title not yet reported: .github/workflows/pr-title.yml is on main but no pull request has been opened since it landed, and on 2026-09-18 there was no open pull request to rebase either, so the context does not exist yet. Add it once a pull request has reported it."
  },
  "Misoto22/astra": {
    "checks": [
      "frontend / node-ci"
    ],
    "note": "pr-title not yet reported: .github/workflows/pr-title.yml is on main but no pull request has been opened since it landed, and on 2026-09-18 there was no open pull request to rebase either, so the context does not exist yet. Add it once a pull request has reported it."
  },
  "Misoto22/lumia-crystal-site": {
    "checks": [
      "pr-title / pr-title"
    ],
    "note": "lint-and-build is unconditional but cannot be required: `pnpm build` reads NEXT_PUBLIC_SHOPIFY_STORE_DOMAIN and NEXT_PUBLIC_SHOPIFY_STOREFRONT_ACCESS_TOKEN from repository secrets, which a Dependabot run never receives, so it throws 'Missing Shopify environment variables' and fails every Dependabot pull request (#4, #5) while passing an ordinary one (#3). claude-review fails on every pull request including the green ones, and the Vercel commit status is red as well. Require lint-and-build once the build tolerates a missing storefront token."
  },
  "Misoto22/erp-modern": {
    "checks": [],
    "note": "No required context: the repository has no .github/workflows directory and no pull requests, so nothing reports."
  },
  "Misoto22/ci": {
    "checks": [
      "actionlint",
      "shellcheck",
      "yamllint"
    ],
    "note": "pr-title not yet reported: .github/workflows/pr-title.yml here is the reusable definition (workflow_call), not a caller, so this repository produces no pr-title / pr-title context of its own."
  }
}
